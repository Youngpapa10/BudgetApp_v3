using System;
namespace CourseAssignment
{
	public class Expense: Budget
	{
		//DateOnly ExpenseDate;

		public Expense(string name, decimal amount): base (name, amount)
		{
			//ExpenseDate = expenseDate;
		}

		
    }
}

